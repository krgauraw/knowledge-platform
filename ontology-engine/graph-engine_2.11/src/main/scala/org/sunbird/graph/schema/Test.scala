package org.sunbird.graph.schema

import java.util.concurrent.CompletionException

/*import javax.inject.Inject
import org.sunbird.graph.schema.DefinitionNode.updateRelationMetadata

import scala.concurrent.ExecutionContext*/

object Test {
  /*implicit val ec: ExecutionContext = new ExecutionContext {
    override def execute(runnable: Runnable): Unit = ???

    override def reportFailure(cause: Throwable): Unit = ???
  }

  def main(args: Array[String]): Unit = {
    val definition = DefinitionFactory.getDefinition("domain", "category", "1.0")
    val inputNode = definition.getNode(request.getRequest)
    definition.validate(inputNode, "create", true) recoverWith { case e: CompletionException => throw e.getCause}
  }*/
}
